debug_mode: False
use_log: True
robot_id: 2  # 0-9

role_info:
    is_client: True
    id: 'drone-02'

network_info:
    ip: '127.0.0.1'

    target_info:
        ip: '172.16.10.3'
        #ip: '127.0.0.1'

message_info:
    state:
        is_tcp: True
        port: 31000
    img:
        is_tcp: True
        port: 32000
        width: 640
        height: 480
    cmd:
        is_tcp: True
        port: 33000
